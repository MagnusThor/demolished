const express=require("express"),app=express();app.use("/demolished",express.static(".")),app.use("/vendor",express.static("node_modules")),app.use("/build",express.static("build")),app.use("/libs",express.static("libs")),app.listen(3e3,function(){console.log("#now serving demolished 0.4  on port 3000")});